# Forecast Lab 内网部署上线指南

> 面向 parkerzhang。代码侧改造已完成并本地实测通过(见《本地集成验证-README.md》)。
> 本文是**只能由你在公司系统操作**的部分: 申请工单 → 配置 → 部署。按顺序勾选即可。

应用基线信息(填工单/部署时用):
- 应用中文名: **Forecast Lab — 网约车财务预测评测台**
- 域名: **forecast.intra.xiaojukeji.com**
- 回调地址: **https://forecast.intra.xiaojukeji.com/sso/callback**
- 负责人: parkerzhang
- 镜像: hub.xiaojukeji.com/parkerzhang/forecast:v1

---

## 阶段 0 · 推镜像(可现在做, 不依赖任何工单)

```bash
cd /Users/didi-project/forecast
# 生产用公司内网基础镜像 + 内网 PyPI(在 Dockerfile/uv 里已留好口子)
make docker-build      # = docker build --platform linux/amd64 -t hub.xiaojukeji.com/parkerzhang/forecast:v1 .
make docker-push
```

- [ ] 镜像已推到 hub.xiaojukeji.com/parkerzhang/forecast:v1

> 若构建机在内网拉不到 docker hub 基础镜像, 加:
> `docker build --platform linux/amd64 --build-arg BASE_IMAGE=hub.xiaojukeji.com/base/python:3.12-slim-bookworm -t ... .`

---

## 阶段 1 · 三个工单(并行提交, 各 1~3 工作日)

### 1.1 SSO 接入(BPM)

链接: https://bpm.didichuxing.com/process/form/bykey/sso_upm_app_add_v3?tenantId=SSO

| 字段 | 填写值 |
|---|---|
| 子系统名称 | Forecast Lab — 网约车财务预测评测台 |
| 主页地址 | https://forecast.intra.xiaojukeji.com/ |
| 回调地址 | https://forecast.intra.xiaojukeji.com/sso/callback |
| 管理员账号前缀 | parkerzhang |
| 系统所属环境 | 线上 |
| 是否开放权限申请 | 否(员工凭 LDAP 直接登录) |
| 业务线/部门 | 按实填 |

- [x] 已提交, 审批通过后记下邮件下发的 **SSO_APP_ID / SSO_APP_KEY**
  - SSO_APP_ID = `2102571` (APP_KEY 不入文档, 只配 OE 环境变量)

### 1.2 内网域名(BPM)

链接: https://bpm.didichuxing.com/process/form/bykey/information_security_domain_insert?tenantId=BPM

| 字段 | 填写值 |
|---|---|
| 申请域名 | forecast.intra.xiaojukeji.com |
| 业务线 | **麒麟** |
| 部署机房 | 与 OE 部署机房**一致**(关键) |
| 解析目标 | OE 部署单元 IP/端口(解析到麒麟 router) |

- [ ] 已提交, 域名与 1.1 回调地址**完全一致**

### 1.3 安全评估(SDL, 与域名并行)

链接: https://sdl.xiaojukeji.com/sdl/dorado

- [ ] 已提交安全评估单

### 1.4 MySQL RDS(数据库平台)

- [ ] 已申请, 记下: host / port / database(建议 forecast_lab) / user / password
- [ ] RDS 机房与 OE 部署机房**同机房**

---

## 阶段 2 · 生成密钥(本地一条命令)

```bash
python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
```

- [ ] 记下输出作为 **SSO_COOKIE_SECRET**(只配在 OE 环境变量, 不入仓/不进群)

---

## 阶段 3 · OE 部署单元

- [ ] 创建部署单元, 镜像填 hub.xiaojukeji.com/parkerzhang/forecast:v1
- [ ] 规格 ≥ **4C8G**(训练吃 CPU)
- [ ] 挂**持久卷**到容器 `/data`(必须, 否则重启丢数据)
- [ ] 容器监听 80, 对接麒麟 router(HTTPS 一般由 router 终止)

### 环境变量(在 OE 部署单元里配)

```
DATABASE_URL=mysql+pymysql://<USER>:<PASS>@<RDS_HOST>:3306/forecast_lab?charset=utf8mb4
FORECAST_LAB_RUNTIME_DIR=/data
MAX_CONCURRENT_TRAINING=3

SSO_APP_ID=2102571
SSO_APP_KEY=<1.1 下发, 只配 OE>
SSO_COOKIE_SECRET=<阶段2 生成>
SSO_LOGIN_BASE=http://mis.diditaxi.com.cn/auth/sso/login
CALLBACK_SCHEME=https
```

- [x] 以上变量已配 (SSO_APP_ID 已确认, 其余待 OE 部署时填入)
- [ ] **未**设置 SSO_DEV_FAKE_USER / FORECAST_LAB_DEV_USER(生产必须留空)
- [ ] **未**设置 SSO_CHECK_CODE_URL / SSO_CHECK_TICKET_URL(留空走默认 mis.diditaxi.com.cn)

> check_code/check_ticket/logout 三个 URL 不配时, 自动用 mis.diditaxi.com.cn 默认值。

---

## 阶段 4 · 首次联调(灰度)

- [ ] 部署起来后访问 https://forecast.intra.xiaojukeji.com/ → 应跳到公司 SSO 登录页
- [ ] 用自己账号登录 → 回到应用, 左侧栏显示「当前用户：parkerzhang」
- [ ] 跑通一次创建实验 → 训练 → 看结果(用 sample_data/ 里的 xlsx)
- [ ] 拉 1~2 个同事登录, 确认互相看不到对方实验
- [ ] 确认后开放给目标 10~50 人

### 如果登录后报错/进不去(最可能是 SSO 返回字段不匹配)

sidecar 已对常见字段(username/user/empId/email/loginName, errno/code/ret 等)做宽容匹配,
大概率开箱即用。万一公司 SSO 返回结构特殊导致取不到用户名:

1. 看容器日志: `OE 控制台日志` 或 `docker logs <容器>`
2. 把 SSO check_ticket 的真实返回 JSON 发我, 我 5 分钟内对齐 `deploy/sso_sidecar/main.py` 的解析

---

## 我(SmartWork)已完成的代码侧

- 用户级数据隔离(repository SQL 强制 owner + 文件按 LDAP 分桶)
- nginx auth_request 前置 SSO + X-SSO-User 透传
- sso-sidecar(三接口, 字段宽容解析)
- MySQL 适配 + 幂等建表/补列
- 单容器编排(nginx+sidecar+streamlit) + Dockerfile(CPU torch, 镜像瘦身)
- 关闭 Streamlit 开发者工具栏(toolbarMode=minimal, 防误触清缓存)
- 本地 docker-compose 全链路实测通过

## 需要你拍板/提供给我的(目前仅 1 项可能需要)

- 联调时若 SSO 返回字段特殊 → 把真实返回 JSON 给我对齐(其他都已自动兼容)
